spring:
  profiles:
    active: dev
  application:
    name: flour-mill-inventory

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
